name: cupertino_anchor
description: A production-ready, zero-flicker Cupertino-style dropdown and picker package for Flutter with intelligent dynamic positioning, safe-area awareness, and native iOS popover aesthetics.
version: 1.0.0
repository: https://github.com/g2god/cupertino_anchor

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
